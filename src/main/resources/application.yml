spring:
  thymeleaf:
    cache: false

server:
  port: ${SERVER_PORT:8081}
  servlet:
    session:
      timeout: 365d

backend:
  admin-api:
    base-url: ${BACKEND_ADMIN_API_BASE_URL:http://localhost:8090}
    internal-key: ${INTERNAL_API_KEY:local-admin-web-key}
